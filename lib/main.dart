import 'package:flutter/material.dart';

import 'try.dart';

void main() => runApp(CGPACAL());

class CGPACAL extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      theme: ThemeData.light(),
      home: MyApp(),
    );
  }
}

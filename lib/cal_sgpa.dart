import 'package:flutter/cupertino.dart';

class calSgpaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Text SGPA page'),
    );
  }
}
